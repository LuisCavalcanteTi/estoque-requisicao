/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UpedidosFinalizadosService } from './UpedidosFinalizados.service';

describe('Service: UpedidosFinalizados', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UpedidosFinalizadosService]
    });
  });

  it('should ...', inject([UpedidosFinalizadosService], (service: UpedidosFinalizadosService) => {
    expect(service).toBeTruthy();
  }));
});
